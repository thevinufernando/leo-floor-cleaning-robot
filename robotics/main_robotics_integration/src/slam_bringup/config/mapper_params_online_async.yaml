# slam_toolbox — online asynchronous mapping parameters.
#
# Tuned for the round differential-drive cleaning robot: RPLIDAR C1 (~12 m
# range, 360 deg) on `laser`, wheel-only odometry publishing odom->base_link
# from mcu_bridge. slam_toolbox owns map->odom.
#
# Frames must match the rest of the stack:
#   odom_frame  = odom            (mcu_bridge parent TF / Odometry frame)
#   base_frame  = base_footprint  (mcu_bridge child TF; REP-105 ground
#                                  projection and the URDF root)
#   map_frame   = map             (slam_toolbox output)
#   scan topic  = /scan           (rplidar_ros, frame_id: laser)
#
# Full TF chain while mapping:
#   map -> odom -> base_footprint -> base_link -> laser/imu/...
#   \____________/  \____________/  \________________________/
#    slam_toolbox     mcu_bridge         robot_state_publisher
slam_toolbox:
  ros__parameters:
    # ---- Plugin / solver ----
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # ---- Frames & topics ----
    odom_frame: odom
    map_frame: map
    base_frame: base_footprint
    scan_topic: /scan
    use_map_saver: true
    mode: mapping   # mapping | localization

    # ---- General ----
    # Async mode: process the most recent scan and drop backlog rather than
    # queueing, so mapping keeps up on the Pi in real time.
    debug_logging: false
    throttle_scans: 1
    transform_publish_period: 0.02   # 50 Hz map->odom
    map_update_interval: 5.0
    resolution: 0.05                 # 5 cm grid — good for indoor cleaning
    min_laser_range: 0.15            # RPLIDAR C1 practical near limit (m)
    max_laser_range: 12.0            # RPLIDAR C1 max usable range (m)
    minimum_time_interval: 0.5
    transform_timeout: 0.2
    tf_buffer_duration: 30.0
    stack_size_to_use: 40000000
    enable_interactive_mode: true

    # ---- Motion filter: add a node when the robot has moved enough ----
    use_scan_matching: true
    use_scan_barycenter: true
    minimum_travel_distance: 0.2     # m
    minimum_travel_heading: 0.2      # rad
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5
    loop_search_maximum_distance: 3.0
    do_loop_closing: true
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # ---- Correlation (scan matching) search space ----
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # ---- Loop-closure correlation search space ----
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # ---- Scan matcher penalties ----
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
