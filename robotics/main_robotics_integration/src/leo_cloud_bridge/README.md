# leo_cloud_bridge

ROS 2 node on the Pi: outbound WSS to leo-relay, `/map` → PNG, soft-arm gate on `cmd_vel_in` → `/cmd_vel`.

- **Pi agent setup handoff:** [PI_ROS_HANDOFF.md](PI_ROS_HANDOFF.md)
- App + server runbook: `leo-application/DEMO_RUNBOOK.md`
